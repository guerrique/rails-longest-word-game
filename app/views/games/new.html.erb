<h1>The longest word!</h1>
<p><% @letters_array.each do |letter| %>
    <%= letter %>
  <% end %>
</p>
<p>

  <form action="/score" method="post" class="form-inline">
    <div class="form-group mx-sm-3 mb-2">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters_array %>
  <input type="text" name="reply" placeholder="Your reply" class="form-control">
  </div>
  <button type="submit" class="btn btn-primary mb-2">Check your score!</button>
</form>

</p>
